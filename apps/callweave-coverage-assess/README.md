# Callweave coverage-assess compatible test app

This app bundle exercises the host-integrated compatible-mode component
for `callweave.coverage-assess`. It validates and registers a thin wrapper
around the durable coverage-report governance contract.

Generate these files with:

```bash
node scripts/generate_callweave_coverage_assess_app.mjs
```
