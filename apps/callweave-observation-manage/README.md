# Callweave observation-manage compatible test app

This app bundle exercises the host-integrated compatible-mode component
for `callweave.observation-manage`. It validates and registers a thin wrapper
around the versioned observation governance contract.

Generate these files with:

```bash
node scripts/generate_callweave_observation_manage_app.mjs
```
