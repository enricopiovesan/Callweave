# Callweave evidence-retain compatible test app

This app bundle exercises the host-integrated compatible-mode component
for `callweave.evidence-retain`. It validates and registers a thin wrapper
around the governed retention decision contract.

Generate these files with:

```bash
node scripts/generate_callweave_evidence_retain_app.mjs
```
