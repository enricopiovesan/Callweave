# Callweave daily-close compatible test app

This app bundle exercises the host-integrated compatible-mode component
for `callweave.daily-close`. It validates and registers a thin wrapper
around the append-only local-day close contract.

Generate these files with:

```bash
node scripts/generate_callweave_daily_close_app.mjs
```
