# Callweave unknown-organize compatible test app

This app bundle exercises the host-integrated compatible-mode component
for `callweave.unknown-organize`. It validates and registers a thin wrapper
around the governed local unknown-clustering contract.

Generate these files with:

```bash
node scripts/generate_callweave_unknown_organize_app.mjs
```
