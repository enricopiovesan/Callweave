# Callweave daily-revise compatible test app

This app bundle exercises the host-integrated compatible-mode component
for `callweave.daily-revise`. It validates and registers a thin wrapper
around the immutable daily canvas revision contract.

Generate these files with:

```bash
node scripts/generate_callweave_daily_revise_app.mjs
```
