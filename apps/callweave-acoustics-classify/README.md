# Callweave acoustics-classify compatible test app

This app bundle exercises the host-integrated compatible-mode component
for `callweave.acoustics-classify`. It validates and registers a thin
wrapper around the prepared-audio classification contract.

Generate these files with:

```bash
node scripts/generate_callweave_acoustics_classify_app.mjs
```
