# Callweave audio-capture compatible test app

This app bundle exercises the host-integrated compatible-mode component
for `callweave.audio-capture`. It validates and registers a thin wrapper
around the recording capture contract.

Generate these files with:

```bash
node scripts/generate_callweave_audio_capture_app.mjs
```
