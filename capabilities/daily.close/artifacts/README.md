# daily-close artifact

`daily-close.wasm` is generated by `../build-fixture.sh`.

The artifact is checked in as deterministic package evidence for the current
pure capability spike. Rebuild it after changing `src/agent.rs`.
